<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
	<link rel="stylesheet" href="/static/theme.css">
    <link rel="stylesheet" href="/static/custom.css">
    <title>A Programmer's Log</title>
    </head>
    <body>
    <header>
        <nav>
        <ul><li><strong><a href="/">A Programmer's Log</a></strong></li></ul>
        </nav>
    </header>

    <main>
    <dl>
      <% @categories.each do |category| %>
      <details>
	<summary><%= category.name.capitalize %></summary>
	<ul>
	<% category.pages.first(5).each do |page| %>
	    <li><a href="/<%= page.href %>"><%= page.title %></a>&nbsp<em><%= page.date %></em></dd>
        <% end %>
        <% if category.pages.length > 5 %>
            <li><em><a href="/categories/<%= category %>">More</a></em></dd>
        <% end %>
        </ul>
      </details>
      <% end %>
    </dl>
    </body>
</html>
